<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginLeft="12dp"
    android:layout_marginBottom="12dp"
    >
    <CheckBox
        android:id="@+id/writesecond_who_text_button"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:text="+"
        android:gravity="center"
        android:button="@color/transparent"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:background="@drawable/radio_select_pww"
        android:textColor="@drawable/select_radio_pww_text"
        android:fontFamily="@font/suit_regular"
        tools:ignore="MissingConstraints" />

    <ImageButton
        android:id="@+id/writesecond_who_delete_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_add_block_delete"
        android:background="#00ff0000"
        app:layout_constraintEnd_toEndOf="@id/writesecond_who_text_button"
        app:layout_constraintTop_toTopOf="@id/writesecond_who_text_button"
        app:layout_constraintBottom_toBottomOf="@id/writesecond_who_text_button"
        android:layout_marginEnd="8dp"
        android:visibility="gone" />


</androidx.constraintlayout.widget.ConstraintLayout>